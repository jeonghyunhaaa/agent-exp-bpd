# exp2: 질문 간 기억(memory)을 활용한 BPD 탐지 정확도 파일럿

## 실험 의도

BPD는 멀티에이전트 시스템의 통신을 부호가 있는 그래프로 모델링하고, 역전파를 통해 악의적인 에이전트를 탐지한다. 이 실험은 다음 가설을 검증하기 위한 것이다.

> 이전 질문들에서 관찰된 악성 에이전트 정보를 다음 질문에 활용하면, 매 질문을 독립적으로 평가하는 기존 BPD보다 더 정확하게 악성 에이전트를 탐지할 수 있다.

즉, 여러 질문에 걸쳐 같은 에이전트 집합이 반복적으로 협업하는 상황에서 "기억"을 도입하는 것이 탐지 성능을 실제로 개선하는지를 확인하는 것이 목표다.

## 기존 연구(BPD)의 한계

기존 BPD 파이프라인은 질문을 하나씩 완전히 독립적으로 처리한다.

- `run_bpd()`는 매 질문마다 처음부터 다시 계산되며, 이전 질문의 판정 결과나 점수를 다음 질문에 전달할 방법이 없다.
- 악성 에이전트의 정체는 실험 설정상 고정되어 있지만, 탐지 알고리즘 자체는 그 사실을 전혀 활용하지 않는다 — 매번 "새로운 상황"으로 취급한다.
- 그 결과, 한 질문에서 증거가 약해 탐지 임계값(epsilon)을 넘지 못하면 그 정보는 그냥 버려진다. 같은 에이전트가 여러 질문에 걸쳐 반복적으로 의심스러운 행동을 보여도, 그 누적된 정황이 판정에 반영되지 않는다.
- 반대로, 탐지된 이후의 대응(격리/복구)도 매 질문 단위로 리셋되기 때문에, "한 번 의심되었던 에이전트를 다음 질문에서 더 주의 깊게 본다"는 개념 자체가 없다.

정리하면, 기존 BPD는 **단일 질문 내에서의 공간적(누가 누구와 다른가) 편차 탐지**에는 강하지만, **여러 질문에 걸친 시간적(누가 반복적으로 의심스러운가) 정보**를 전혀 활용하지 못한다는 한계가 있다.

## 실험 설계 방안

### 핵심 메커니즘

기존 `detect_malicious`는 에이전트별로 "다른 모든 에이전트와의 평균 쌍별 점수 편차(gap)"를 계산해, 가장 큰 값이 epsilon을 넘으면 그 에이전트를 악성으로 판정한다. 이 gap에 **이전 질문들로부터 쌓인 누적 신뢰(의심) 점수**를 더해서 판정하도록 확장한다.

- 누적 신뢰 점수는 EMA(지수이동평균) 방식으로 갱신한다 — 최근 질문의 증거일수록 더 크게 반영하고, 과거 증거는 시간이 지나며 서서히 잊혀진다. 한 번 의심된 에이전트를 영구히 블랙리스트에 올리는 방식은 채택하지 않는다.
- 판정에 쓰이는 값은 "이번 질문의 새 증거 + 이전까지 누적된 의심"의 합이며, 누적 신뢰 자체는 항상 그 질문의 순수한(가공되지 않은) 증거로만 갱신한다 — 판정 결과가 다시 누적치를 왜곡시키는 자기참조를 방지하기 위함이다.
- 기존 방식(누적 신뢰를 전혀 반영하지 않는 경우)과 비교했을 때 완전히 동일한 결과가 나오는지를 회귀 기준으로 삼아, 새 메커니즘이 기존 판정 로직을 그대로 포함하는 상위 집합임을 보장한다.

### 검증 시나리오

같은 질문 시퀀스를 여러 조건에서 반복 수집해 비교한다.

1. **악성 에이전트 고정** — 시퀀스 내내 같은 에이전트가 악성으로 행동. 가설을 검증하는 핵심 조건으로, 기존 방식 대비 더 빠르고 정확하게 잡아내는지를 본다.
2. **악성 에이전트 교체** — 시퀀스 중간에 악성 에이전트가 바뀜. 누적 신뢰가 특정 에이전트에 고착되어 새로운 범인을 놓치지 않는지, 즉 메커니즘의 적응성을 확인한다.
3. **공격 없음(대조군)** — 악성 에이전트가 아예 없는 상황. 누적 신뢰가 시간이 지나며 노이즈만으로 잘못된 의심(false positive)을 쌓아가지 않는지를 확인한다.

세 시나리오 모두 이번 파일럿에서는 하나의 MAS 구조(에이전트들이 서로 자유롭게 의견을 주고받는 형태)로 한정해 진행하며, 오류 전파를 사전에 차단하는 후속 메커니즘(게이트)은 이번 실험 범위에 포함하지 않는다 — 먼저 "기억을 활용하면 탐지 정확도가 실제로 개선되는가"라는 가장 기본적인 질문에 대한 답을 얻는 것이 우선이다.

### 평가 방법

같은 수집 데이터에 대해 기존 방식과 메모리 방식을 나란히 재계산해서 비교한다 (LLM을 다시 호출해 생기는 우연한 차이를 배제하고, 알고리즘 자체의 차이만 보기 위함). 핵심적으로 확인하려는 것은 다음 세 가지다.

- 악성 에이전트를 **더 적은 질문 수 안에** 잡아내는가 (탐지 속도)
- 전체 시퀀스에 걸친 **탐지 정확도(recall)**가 실제로 높아지는가
- 그 과정에서 **잘못된 의심(false positive)**이 함께 늘어나지는 않는가
