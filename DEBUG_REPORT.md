# exp2 디버그 실행 결과 보고서

## 목적

`exp2/collect_sequence.py → exp2/analyze_sequence.py` 파이프라인이 실제 LLM 호출까지 포함해 에러 없이 끝까지 도는지 확인하기 위한 초소형 실행. **가설을 검증하기 위한 실행이 아니며, 아래 수치는 참고용일 뿐 통계적으로 의미를 둘 수 없다.**

## 실행 규모

- 시나리오 A(고정) 1개 리플리케이트, B(교체) 1개 리플리케이트, C(공격없음) 1개 리플리케이트
- 리플리케이트당 질문 3개 → 총 질문 9개
- flat 구조(5명), model = `gpt-4o-mini`

## 결과 1: 파이프라인 검증 — 통과

수집 → 분석 → 시각화까지 전 구간이 에러 없이 완료되었고, 다음 산출물이 정상 생성되었다.

- `sequence_raw.pkl` (9개 질문 원본 데이터)
- `table_recall_fpr.csv` (epsilon/decay/mu 전 구간 스윕 결과)
- `fig_rolling_recall.png`, `fig_scenario_B_recovery.png`, `fig_rolling_fpr_control.png`

## 결과 2: 베이스라인(기존 방식) 성능 — epsilon에 매우 민감

| epsilon | A_fixed recall | C_noattack FPR | B_switch pre recall | B_switch post recall |
|---|---|---|---|---|
| 0.50 | 0.667 | 0.667 | 1.000 | 0.500 |
| 0.75 | 0.667 | 0.667 | 1.000 | 0.500 |
| 1.00 | 0.333 | 0.667 | 0.000 | 0.500 |
| 1.25 | 0.000 | 0.000 | 0.000 | 0.000 |
| **1.50 (논문 기본값)** | **0.000** | **0.000** | **0.000** | **0.000** |
| 1.75 | 0.000 | 0.000 | 0.000 | 0.000 |
| 2.00 | 0.000 | 0.000 | 0.000 | 0.000 |

**예상 밖의 발견:** 논문 기본값 epsilon=1.5에서는 이 데이터에서 베이스라인이 단 한 건도 탐지하지 못했다(recall=0). flat 구조(5명)의 `agent_scores`가 거의 ±0.5로 이산적으로만 나와서, 최대 gap이 1.0을 잘 넘기지 못하는 것으로 보인다. epsilon을 0.5~1.0으로 낮추면 탐지는 되지만, 그만큼 공격이 없는 상황(C_noattack)에서도 FPR이 0.667까지 치솟는다 — 이 표본 규모에서는 recall과 FPR을 동시에 만족하는 epsilon 구간이 뚜렷하지 않다.

## 결과 3: 메모리 방식이 베이스라인을 앞서는 지점 (epsilon≈1.25)

베이스라인이 전혀 못 잡는 epsilon=1.25 구간에서, 메모리(EMA 누적 신뢰) 방식은 `mu >= 1.0`일 때 A_fixed 3개 리플리케이트 중 1개를 추가로 잡아냈다(recall 0 → 0.333). B_switch post 구간에서도 0 → 0.5~1.0으로 개선되는 조합이 다수 관찰됐다.

| decay | mu | A_fixed recall | C_noattack FPR | B_switch post recall |
|---|---|---|---|---|
| 0.30 | 1.0 | 0.333 | 0.000 | 0.500 |
| 0.30 | 1.5 | 0.333 | 0.000 | 1.000 |
| 0.30 | 2.0 | 0.333 | 0.333 | 1.000 |
| 0.50 | 1.0 | 0.333 | 0.000 | 0.500 |
| 0.50 | 2.0 | 0.333 | 0.000 | 1.000 |
| 0.70 | 2.0 | 0.333 | 0.000 | 0.500 |

가설이 예측한 방향(메모리를 쓰면 베이스라인이 놓치는 사례를 잡아낸다)과 정확히 일치하는 패턴이지만, **리플리케이트 3개 중 1개**가 바뀐 것이라 우연과 통계적으로 구분되지 않는다. `mu`가 너무 커지면(2.0, decay=0.3) FPR도 함께 올라가기 시작하는 트레이드오프 조짐도 보인다.

## 한계

- 시나리오별 리플리케이트가 1~3개뿐이라 모든 recall/FPR 수치는 참고용. 실제 결론을 내리려면 계획된 파일럿 규모(리플리케이트 3개 이상, 질문 10개/시퀀스)가 필요하다.
- `gpt-4o-mini`가 채점 프롬프트(`'[score] x'`) 형식을 자주 어겨 재시도가 발생, 실제 LLM 호출 수가 이론치(630회)의 약 3.3배(2,070회)로 늘어났다. 실제 파일럿(이론치 6,300회)도 이 배율이 적용되면 실행 시간·비용이 예상보다 커질 수 있다.

## 다음 단계 제안

1. epsilon=1.5 고정 대신, 계획대로 epsilon을 스윕해서 이 데이터셋/모델 조합에 맞는 구간을 실제 파일럿에서도 함께 보고한다.
2. 리플리케이트 3개, 질문 10개짜리 실제 파일럿(총 90문항)을 실행해 위에서 관찰된 방향성이 통계적으로 유의미한 수준으로 재현되는지 확인한다.
