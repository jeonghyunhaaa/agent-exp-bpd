# exp2: 질문 간 기억(memory)을 활용한 BPD 탐지 정확도 파일럿

### 동기

- **이전 질문에서 탐지한 악의적 에이전트에 대한 정보를 다음 질문에서 활용할 수 있을까?**


### 기존 방식의 한계

- 논문에서는 각 질문바다 처음부터 다시 악의적 에이전트를 탐지함. 
- 같은 에이전트가 여러 질문에 걸쳐 반복적으로 의심스러운 행동을 보여도 다음 판정에 전혀 반영되지 않음. 
- 매 번 질문을 받을 때마다 확인한 정보는 버려짐.

정리하면 BPD는 **한 질문 안에서 "누가 남들과 다른가"를 보는 데는 강하지만, 여러 질문에 걸쳐 "누가 반복적으로 의심스러운가"는 전혀 보지 못한다.**

## 실험 설계 방안

### 핵심 아이디어

- 기존 로직
  - 에이전트 별로 "다른 에이전트들과 얼마나 다른가"를 계산해서 기준치(epsilon)을 넘으면 악성으로 판정
- 새로운 로직
  - 기존 로직은 유지하고, 이에 **"이전 질문에서 쌓인 누적 의심 점수"**를 더해서 판정하도록 확장
기존 판정 로직 — 에이전트별로 "다른 에이전트들과 얼마나 다른가"를 계산해서 기준치(epsilon)를 넘으면 악성으로 판정
  - 누적 의심 점수는 최근 질문일수록 더 크게 반영되고 오래 된 건 서서히 잊는 방식으로 관리(지수이동평균, EMA)
  ```trust_new[a] = decay * trust_old[a] + (1 - decay) * gap_raw[a]```
  - 과거 이력을 얼마나 적극적으로 반영할지는 조절 가능한 값(`mu`)으로 두고, `mu = 0`이면 기존 방식과 완전히 동일하게 동작



### 검증 시나리오

| 시나리오 | 내용 | 확인하려는 것 |
|---|---|---|
| **악성 에이전트 고정** | 여러 질문에 걸쳐 같은 에이전트가 계속 악성 | 가설을 검증하는 핵심 조건 — 누적 정보가 실제로 탐지를 돕는가 |
| **악성 에이전트 교체** | 중간에 범인이 바뀜 | 메모리가 옛 범인에 고착돼서 새 범인을 놓치지 않는가 (적응성) |
| **공격 없음 (대조군)** | 악성 에이전트가 아예 없음 | 누적 의심 점수가 시간이 지나며 노이즈만으로 헛다리를 짚지 않는가 |

### 평가 내용

같은 데이터에 대해 기존 방식과 메모리 방식을 나란히 재계산해서 비교한다 (LLM을 다시 불러서 생기는 우연한 차이를 배제하고, 알고리즘 차이만 보기 위함)

1. 더 빨리 탐지하는가?
2. 전체 탐지 **정확도**가 실제로 오르는가?
3. 그 과정에서 **무고한 에이전트를 잘못 의심하는 일**이 함께 늘어나지는 않는가?


## 지금까지 진행 상황

- 질문 9개로 진행
- 결과

| epsilon | A_fixed recall | C_noattack FPR | B_switch pre recall | B_switch post recall |
|---|---|---|---|---|
| 0.50 | 0.667 | 0.667 | 1.000 | 0.500 |
| 0.75 | 0.667 | 0.667 | 1.000 | 0.500 |
| 1.00 | 0.333 | 0.667 | 0.000 | 0.500 |
| 1.25 | 0.000 | 0.000 | 0.000 | 0.000 |
| **1.50 (논문 기본값)** | **0.000** | **0.000** | **0.000** | **0.000** |
| 1.75 | 0.000 | 0.000 | 0.000 | 0.000 |
| 2.00 | 0.000 | 0.000 | 0.000 | 0.000 |

    - 논문 기본값 epsilon=1.5는 유의미하지 않음
    - epsilon=0.5~1.0으로 낮추면 탐지는 되지만 공격이 없는 상황(C)에서 FPR 높아짐(잘못 탐지)
    - 추후 epsilon별 결과 관찰 필요

